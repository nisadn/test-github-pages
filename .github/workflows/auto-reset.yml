name: Auto Reset
run-name: Reset test branch
on:
  push:
    branches:
      - feat/test-auto-reset

jobs:
  upsert:
    environment: auto-reset
    steps:
      - name: testing steps
        run: echo "This is a test step"
