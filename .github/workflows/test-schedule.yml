name: 'Test Scheduled'
env:
  RESET_BRANCH: 'this-is-a-branch'
on:
  schedule:
    - cron: '0 10 * * *' # testing dev branch

jobs:
  testing:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Hello World ${{ env.RESET_BRANCH }}"