name: "[Testing] Request to Publish"

on:
  push:
    branches:
      - testing/push

jobs:
  make-changes:
    name: Make changes to trigger publish
    uses: ./.github/workflows/changes-another-workflow.yml
  create-pull-request:
    name: Create Pull Request to publish
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      - name: Create Pull Request
        uses: peter-evans/create-pull-request@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          title: "Automated PR"
          commit-message: "Update"
          branch: "publish-rc"
          base: ${{ github.ref_name }}
