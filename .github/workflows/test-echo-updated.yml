name: Test Echo - Cache

on:
  push:
    branches:
      - release/*

jobs:
  echo_input:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/github-script@v7
        with:
          script: |
            console.log(`Echo to test caching - ${new Date().toISOString()}`);
