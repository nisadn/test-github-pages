name: Publish Workflow

on:
  workflow_dispatch:

jobs:
  publish:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run some dummy tasks
        run: echo "Running dummy tasks..."

      - name: Call deploy-storybook
        uses: ./.github/workflows/deploy-storybook.yml
        if: ${{ !cancelled() }}
        with:
          checkout_branch: 'main'